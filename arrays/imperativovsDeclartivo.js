// Immperativo -> seria usar um for pra fazer uma media de notas por exemplo
// é mais importante como tem que ser feito

// Declarativo -> seria usar map/reduce/filter para executar essa função
// é mais importante o que tem que ser feito


const getNota = aluno => aluno.nota
const soma = (total, atual) => total + atual
const mediaNotas = alunos.map(getNota).reduce(soma) 