//OO
// Dados que dentro desses dados tem funções que manipulam eles

objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        //...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// 1. abstração (o que os objetos possuem(essencia))
// 2. encapsulada (esconder alguns detalhes de implementaçãa "parte de dentro/oculta")
// 3. herança (objeto composto por outro)
// 4. polimorfismo (filho recebe caracteristicas do pai)